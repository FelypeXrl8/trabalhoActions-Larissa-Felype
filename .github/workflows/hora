name: Mostrar Hora do Push
on:
  push:
    branches:
      - main

jobs:
  mostrar_hora:
    runs-on: ubuntu-latest
    steps:
      - name: Exibir hora do push
        run: |
          echo "Data e hora do último push:"
          date -u +"%Y-%m-%d %H:%M:%S %Z"
